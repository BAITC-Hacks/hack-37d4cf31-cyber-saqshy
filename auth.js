window.auth={user:null,async request(url,opts={}){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(opts.headers||{})},...opts}),d=await r.json();if(!r.ok)throw Error(d.error||'Request failed');return d},async load(){let d=await this.request('/api/auth/me');this.user=d.user;this.paint()},paint(){let x=document.querySelector('#auth-area');x.innerHTML=this.user?`<b>${this.user.name}</b> <button class="secondary" id="logout">Выйти</button>`:'<button class="primary" id="login">Войти</button>';document.querySelector('#login')?.addEventListener('click',()=>this.open());document.querySelector('#logout')?.addEventListener('click',async()=>{await this.request('/api/auth/logout',{method:'POST',headers:{'X-CSRF-Token':this.user.csrfToken}});this.user=null;this.paint()})},open(){document.querySelector('#modal-content').innerHTML=`<h2>Безопасный вход</h2><p>Пароль хранится только как hash. Email и имя шифруются в SQLite.</p><form id="auth-form"><label>Имя<input name="name" placeholder="Только для регистрации"></label><label>Email<input required name="email" type="email"></label><label>Пароль<input required name="password" type="password" minlength="12" placeholder="Минимум 12 символов"></label><label>Роль<select name="role"><option value="business">Бизнес</option><option value="student">Студенческая команда</option></select></label><button class="secondary" type="button" id="do-login">Войти</button> <button class="primary">Зарегистрироваться</button><p id="auth-error"></p></form>`;document.querySelector('#modal').classList.remove('hidden');let f=document.querySelector('#auth-form'),go=async(mode)=>{try{let d=Object.fromEntries(new FormData(f));if(mode==='login')delete d.name;this.user=(await this.request('/api/auth/'+mode,{method:'POST',body:JSON.stringify(d)})).user;this.paint();document.querySelector('#modal').classList.add('hidden')}catch(e){document.querySelector('#auth-error').textContent=e.message}};f.onsubmit=e=>{e.preventDefault();go('signup')};document.querySelector('#do-login').onclick=()=>go('login')}};window.auth.load().catch(()=>document.querySelector('#auth-area').innerHTML='<small>Run server.js</small>');
